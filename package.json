{
  "name": "astra-game-arena",
  "version": "0.2.0",
  "description": "A resumable, private-desktop Steam game benchmark arena for Codex models.",
  "type": "module",
  "private": false,
  "license": "MIT",
  "bin": {
    "game-arena": "dist/src/cli.js",
    "parabox-arena": "dist/src/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --import tsx 'test/**/*.test.ts'",
    "dev": "tsx src/cli.ts",
    "doctor": "tsx src/cli.ts doctor",
    "demo": "tsx src/cli.ts demo"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
